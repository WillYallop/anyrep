import navigation from './modules/navigation'
import booking from './modules/booking'

const state = () => ({
  
});

const mutations = {
  
};

const actions = {
  
};

const modules = {
  navigation,
  booking
};

export default {
  state,
  mutations,
  actions,
  modules,
}

